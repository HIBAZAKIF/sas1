const prompt = require('prompt-sync')();

const nom1 = prompt('entrer un nom ? ');
const nom2 = prompt('entrer un autre nom ? ');

if(nom1.length == nom2.length){
    

}else{
    console.log("les mots sont pas anagramme")
}
